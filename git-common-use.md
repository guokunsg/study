## Basic
          [History]
   git reset ↓ ↑ git commit
           [Stage]
git checkout ↓ ↑ git add/stage
      [Working directory]

* Git Flow
Main branches: master, develop
Short branches: feature, hotfix, release
* Github flow
master branch, pull request, merge request

## Commands
* git init:
* git config:
* git add:
* git commit:
* git branch:
* git checkout -b [branch]:
* git merge [branch]:
* git rebase:
* git tag:
* git log:
* git diff:
* git fetch [remote]:
* git pull [remote] [branch]:
* git checkout [file]: Restore file in stage to working directory
* git reset: Reset files in stage
* git revert [commit]: Create a new commit to revert the changes in commit
* git stash: Hide local changes

## Solve conflict
* git stash
 git pull
 git stash pop
* git mergetool: kdiff3
